# To generate a random number between given range

import random

random_number = random.randint(11, 21)
print(random_number)

# To generate a random Floating point number between given range
#gives a float number in the interval [0,1] 
random_float = random.random()
print(random_float)